import auf = require("aurelia-framework");

export class NavBar {
    static annotations = [new auf.Property("router")];

    //attached(...myargs) { alert('im attached :)'); }
    //bind(...myargs) { alert('im bound :)'); }
}